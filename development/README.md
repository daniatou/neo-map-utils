# Development

This folder contains contributor-facing playgrounds.

Projects in this folder are part of the Nx workspace and use the local packages from `packages/`.

## Angular Local Demo

```bash
pnpm nx serve demo-angular-local
```

Use this demo while developing the SDK because changes in local packages are reflected without publishing to npm.

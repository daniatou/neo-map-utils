# Icon Generation

`generate-icons.mjs` extracts icons from bank SVG files and writes TypeScript packs under `src/generated`.

The generator intentionally supports curated manifests so applications can avoid bundling every icon from large banks.

# Icons

Framework-independent SVG registry and generated icon packs.

The SVG bank files at the package root are source assets. Run:

```bash
pnpm icons:generate
```

The generator extracts curated icons from `icon-manifest.json` into `src/generated`.
